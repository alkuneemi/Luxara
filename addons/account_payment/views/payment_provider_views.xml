<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">payment.provider.form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="payment_followup" position="attributes">
                <attribute name="invisible">
                    0
                </attribute>
            </group>
            <group name="payment_followup" position="inside">
                <field name="journal_id"
                    context="{'default_type': 'bank'}"
                    required="code not in ['none', 'custom']"/>
                <field name="support_refund" groups="base.group_no_one"/>
            </group>
            <field name="minimum_amount" position="before">
                <field
                    name="available_pricelist_ids"
                    widget="many2many_tags"
                    placeholder="All Pricelists"
                    options="{'no_create': True}"
                />
            </field>
        </field>
    </record>

    <record id="payment_provider_list" model="ir.ui.view">
        <field name="name">payment.provider.list.account</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_list"/>
        <field name="arch" type="xml">
            <list position="inside">
                <field name="available_pricelist_ids" widget="many2many_tags" optional="hide"/>
                <field name="journal_id" optional="hide"/>
            </list>
        </field>
    </record>

</odoo>
